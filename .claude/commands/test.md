---
description: 运行前端和后端测试套件
allowed-tools: Bash(npm:*,python:*), Read
---

# Test 命令

你的任务是运行项目的测试套件并报告结果。

## 步骤

1. **前端测试**
   ```bash
   cd frontend
   npm test -- --coverage
   ```

2. **后端测试**
   ```bash
   cd backend
   python manage.py test --verbosity=2
   ```

3. **分析覆盖率**
   ```bash
   # 前端覆盖率
   cd frontend
   npm test -- --coverage --watchAll=false

   # 后端覆盖率（需要 coverage.py）
   pip install coverage
   coverage run --source='.' manage.py test
   coverage report
   ```

4. **失败的测试**
   - 识别失败的测试
   - 分析失败原因
   - 提供修复建议

5. **生成报告**
   - 测试通过率
   - 代码覆盖率
   - 性能指标（如果可用）

## 测试目标

- ✅ 所有测试通过
- ✅ 代码覆盖率 > 80%
- ✅ 无跳过的测试
- ✅ 无性能退化

## 输出格式

```markdown
## 测试报告

### 前端测试
- ✅ 通过: 42/42
- 📊 覆盖率: 87%
- ⏱️  耗时: 5.2s

### 后端测试
- ✅ 通过: 35/35
- 📊 覆盖率: 82%
- ⏱️  耗时: 12.8s

### 失败的测试
- [ ] WorkOrderList.test.js: 23
  - 问题: XXX
  - 建议: YYY

### 覆盖率分析
- 需要改进的模块:
  - frontend/src/api/workorder.js: 45%
  - backend/workorder/views.py: 60%
```

## 使用示例

```bash
/test
```
